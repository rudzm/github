name: "Testing"

on:
  workflow_dispatch:

jobs:
  testing:
    runs-on: "ubuntu-latest"
    environment: dev
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with: 
          ref: ${{ github.ref.name }}

      - name: Test
        run: |
          echo $GITHUB_WORKFLOW_REF